[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — all routes go to index.html
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200

# API proxy — forward /api/* to your deployed backend
[[redirects]]
  from   = "/api/*"
  to     = "http://62.84.187.126:4005/api/:splat"
  status = 200
  force  = true

[[redirects]]
  from   = "/ws/*"
  to     = "http://62.84.187.126:4005/ws/:splat"
  status = 200
  force  = true

[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
